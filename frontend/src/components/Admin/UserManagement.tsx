import React from 'react';
import './css/AdminScreen.css'; // CSSファイルをインポート

const UserManagement: React.FC = () => {
  return (
    <div>
      <h2 className="admin-h2">ユーザー管理（社員を追加、削除、更新）</h2>
      {/* ユーザー管理機能をここに追加 */}
    </div>
  );
};

export default UserManagement;
